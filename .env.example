# Google Gemini API key — enables the AI incident narrative in `dfirauto triage`.
# Without it, dfirauto still runs and produces full triage results; it just
# skips the AI narrative/recommendations section (fails gracefully, no crash).
# Get a free-tier key at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=
